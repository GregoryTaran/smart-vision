{
  "name": "smart-vision",
  "version": "1.0.0",
  "description": "Smart Vision — AI Voice Assistant and Dashboard",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "dev": "firebase emulators:start --only hosting,functions",
    "deploy": "firebase deploy --only hosting,functions",
    "deploy:all": "firebase deploy" 
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {},
  "devDependencies": {}
}
